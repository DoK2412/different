Реализация односвязного списка без использования стандартных структур
Python (list, dict, tuple и прочие) и дополнительных модулей на основе классов.

Для структуры реализованы следующие методы:
    append — добавление элемента в конец списка;
    get — получение элемента по индексу;
    remove — удаление элемента по индексу.

Так же списк по итогу можно итерировать с помощью цикла.

